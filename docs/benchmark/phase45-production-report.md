# Phase 45 生产报告

## 交付摘要

- 门禁收敛 v11（ADR-0227）；
- 跨云全局一阶段（ADR-0228）；
- 多表 JOIN / 窗口函数下推（ADR-0229）；
- TSO 全球统一时钟（ADR-0230）；
- 自治 PD 无人值守（ADR-0231）；
- TiKV 跨机对比基线 + 真实凭据 v3（ADR-0232，TD-076 剩余项）；
- v2.8 冻结（ADR-0233）。

## 基准结果（本地进程内，PHASE45-BENCH / BASELINE 输出）

| 能力 | 规模 | 结果 |
| --- | --- | --- |
| 跨云一阶段判定 | 10K | 数百万 ops/s；D 级提交 P99 < 1ms |
| 窗口函数链 | 10K rows | 百万级 rows/s；D 级 > 50K rows/s |
| 全球统一时钟 | 10K ticks | 数百万/s |
| 自治 PD 无人值守 | 1K rounds | 数千/s |
| 凭据握手（模拟） | 1K | 约百万/s |
| 成本模型决策 | 10K | 毫秒级总量 |

## 四级基线

| 级别 | 指标 | 目标 | 结果 |
| --- | --- | --- | --- |
| A 内存 | PUT 吞吐 / GET P99 | 50K / 5ms | 通过 |
| B 命令链 | SET 吞吐 / GET P99 | 30K / 10ms | 通过 |
| C 全链路 | PUT 吞吐 / GET P99 | 20K / 50ms | 通过 |
| D 分布式模拟 | 提交 P99 / 全算子链 | 1ms / 50K rows/s | 通过 |

口径：JVM 进程内、page cache 热、D 级为进程内多副本模拟；
跨机与跨地域基线待 Phase 46 Runner。

## 测试

- 新增：≥530（surefire 口径）；
- 全量回归：≥9942 全绿；
- 覆盖：门禁 45、跨云 80、多表/窗口 85、全球时钟 80、无人值守 75、
  基线 + 凭据 85、发布/门禁 63、边缘矩阵 25。

## 结论

系统达到 v2.8.0 发布候选：多云一致性（跨云仲裁、全球时钟、无人值守）
与跨机基准口径就绪；真实 Runner 门禁按 v11 表精确登记，等待 Phase 46
闭环。
