# Phase 44 生产报告

## 交付摘要

- 门禁收敛 v10（ADR-0220）；
- 全局一阶段提交规模化（ADR-0221，TD-079 规模化）；
- 全算子联合下推（ADR-0222，TD-080 规模化）；
- TSO 跨地域容灾（ADR-0223）；
- 自治 PD 全自动（ADR-0224）；
- TiKV 对比基线 + 真实凭据 v2（ADR-0225，TD-076 关闭方向）；
- v2.7 冻结（ADR-0226）。

## 基准结果（本地进程内，PHASE44-BENCH / BASELINE 输出）

| 能力 | 规模 | 结果 |
| --- | --- | --- |
| 全局一阶段判定 | 10K | 数百万 ops/s；D 级提交 P99 < 1ms |
| 全算子链 | 10K rows | > 2M rows/s；D 级 > 50K rows/s |
| TSO 主备分配 | 1K batches | 数十万 batches/s |
| 自治 PD 自动执行 | 1K rounds | 数千 rounds/s |
| 凭据探测（模拟） | 1K | 约百万 probes/s |
| 门禁注册表查询 | 10K | 毫秒级总量 |

## 四级基线

| 级别 | 指标 | 目标 | 结果 |
| --- | --- | --- | --- |
| A 内存 | PUT 吞吐 / GET P99 | 50K / 5ms | 通过 |
| B 命令链 | SET 吞吐 / GET P99 | 30K / 10ms | 通过 |
| C 全链路 | PUT 吞吐 / GET P99 | 20K / 50ms | 通过 |
| D 分布式模拟 | 提交 P99 / 全算子链 | 1ms / 50K rows/s | 通过 |

口径：JVM 进程内、page cache 热、D 级为多副本进程内模拟；
跨机与跨地域基线待 Phase 45 Runner。

## 测试

- 新增：≥520（surefire 口径）；
- 全量回归：≥9412 全绿；
- 覆盖：门禁 45、全局一阶段 75、全算子 80、TSO 容灾 80、
  自治全自动 75、基线 + 凭据 80+、发布/门禁 55、边缘矩阵 25。

## 结论

系统达到 v2.7.0 发布候选：全球规模能力（全局一阶段、TSO 容灾、
全自动自治）与 TiKV 对比基线就绪；真实 Runner 门禁按 v10 表精确
登记，等待 Phase 45 闭环。
