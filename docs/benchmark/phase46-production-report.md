# Phase 46 生产报告

## 交付摘要

- 门禁收敛 v12（ADR-0234）；
- 跨云一阶段规模化（ADR-0235）；
- 窗口函数全族 / 动态下推（ADR-0236）；
- TSO 跨云授时仲裁 + 防时钟回拨（ADR-0237）；
- 自治无人值守全自动合规证明（ADR-0238）；
- TiKV 跨机基准定期回归 + 真实凭据 v4（ADR-0239，TD-076 剩余项）；
- v2.9 冻结（ADR-0240）。

## 基准结果（本地进程内，PHASE46-BENCH / BASELINE 输出）

| 能力 | 规模 | 结果 |
| --- | --- | --- |
| 规模化一阶段判定 | 10K | 数十万 ops/s；D 级提交 P99 < 1ms |
| 窗口函数全族 | 10K rows | 百万级 rows/s；D 级 > 50K rows/s |
| 跨云授时仲裁 | 10K ticks | 数十万/s |
| 动态规划器 | 10K | 百万级/s |
| 合规审计链 | 10K | 数十万/s；verify 百次毫秒级 |
| 凭据 v4 握手 | 1K | 约百万/s |

## 四级基线

| 级别 | 指标 | 目标 | 结果 |
| --- | --- | --- | --- |
| A 内存 | PUT 吞吐 / GET P99 | 50K / 5ms | 通过 |
| B 命令链 | SET 吞吐 / GET P99 | 30K / 10ms | 通过 |
| C 全链路 | PUT 吞吐 / GET P99 | 20K / 50ms | 通过 |
| D 分布式模拟 | 提交 P99 / 窗口链 | 1ms / 50K rows/s | 通过 |

口径：JVM 进程内、page cache 热、D 级为进程内多副本模拟；
跨机与跨地域回归待 Phase 47 Runner。

## 测试

- 新增：≥540（surefire 口径）；
- 全量回归：≥10491 全绿；
- 覆盖：门禁 45、规模化 80、窗口全族/动态 90、仲裁 80、合规 75、
  基线 + 凭据 95、发布/门禁 55、边缘矩阵 25。

## 结论

系统达到 v2.9.0 发布候选：全球一致性最终化（分层仲裁、窗口全族、
跨云授时、合规自动化）与跨机回归口径就绪；真实 Runner 门禁按 v12
表精确登记，等待 Phase 47 闭环。
