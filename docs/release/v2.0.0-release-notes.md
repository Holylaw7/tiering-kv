# Tiering-KV v2.0.0 Release Notes（GA 里程碑）

## 新能力

- 真实执行门禁收敛表 v3 + JVM 级门禁扩展（ADR-0171）；
- 自学习围栏多目标优化：成本 × 风险 × SLO 加权（ADR-0172）；
- 跨云远端物化存储：远端落盘 + CDC 增量同步 + 主权拒绝
  （ADR-0173）；
- 合规证明跨机构验证：独立验证 + JSON 导出（ADR-0174）；
- 多云 spot 竞价：中断感知期望成本（ADR-0175）；
- 网络策略跨租户审计：编译联动 + 视图聚合（ADR-0176）；
- 多 SLO 预算自动谈判：加权缺口 + 最差优先（ADR-0177）；
- v2.0.0 GA 发布流水线。

## 兼容性

- v1.0–v1.9 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 37 新增 380 项（surefire 口径）；
- 全量回归 **6040/6040 全绿**（+6 容器门控跳过）。

## 已知限制

- 真实执行门禁（CI 容器/磁盘混沌/kind/release/跨机跨地域）待 Runner；
- 远端物化增量状态未持久化；
- spot 中断率为静态估计。
