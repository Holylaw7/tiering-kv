# Tiering-KV v2.3.0 Release Notes

## 新能力

- 真实执行门禁收敛表 v6 + JVM 级门禁扩展（ADR-0192）；
- 多智能体分层联邦学习：拓扑分组 → 分层聚合（ADR-0193，TD-070
  关闭方向）；
- 物化视图冷层对象存储归档：S3 兼容上传/下载/删除（ADR-0194）；
- 合规证明跨链互操作：多链锚定 + 一致性验证（ADR-0195）；
- Spot 市场实时竞价：价格/中断率约束出价（ADR-0196）；
- 自适应加固策略学习：风险反馈 → 阈值自进化（ADR-0197）；
- 全局 Pareto 动态重平衡：指标流 → 前沿更新 + 限幅（ADR-0198）；
- v2.3.0 发布流水线。

## 兼容性

- v1.0–v2.2 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 40 新增 482 项（surefire 口径）；
- 全量回归 **7360/7360 全绿**（+6 容器门控跳过）。

## 已知限制

- 真实执行门禁（CI 容器/磁盘混沌/kind/release/跨机跨地域）待 Runner；
- 对象存储为进程内模拟；
- Spot 竞价未接真实市场做市。
