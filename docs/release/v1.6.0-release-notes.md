# Tiering-KV v1.6.0 Release Notes

## 新能力

- SQL 写 2PC 真实协调器端到端（ADR-0144）；
- 选主与 Raft term 联动，低 term 防脑裂（ADR-0145）；
- 控制台 UI 原型 + RBAC 门控（ADR-0146）；
- SaaS 商业化闭环：订阅状态机 + 市场目录 + 周期计费（ADR-0146）；
- AI 驱动容量规划：趋势预测 + 置信带 + 风险等级（ADR-0147）；
- 数据网格联邦查询：域目录 + 跨域聚合 + JOIN（ADR-0148）；
- 全球多活流量治理：地域配额 + 优先级降级（ADR-0149）。

## 兼容性

- v1.0–v1.5 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 33 新增 266 个测试；
- 全量回归 0 failures（4251 → ≥4450）。

## 已知限制

- 跨地域真实基准、K8s 容器 E2E、真实磁盘混沌待 Linux Runner；
- 控制台 UI 为原型，无实时推送；
- AI 容量预测为线性/指数模型。
