# Tiering-KV v1.3.0 Release Notes

2026-08-11 · Phase 30 · Dynamic Resharding & Global Operations

## 本版本新增

- 动态重分片（ADR-0126）：版本化路由 + 双写窗口 + 原子切换 + 回滚；
- 向量分片迁移（ADR-0127）：逐 id 迁移 + 校验 + 查询不中断；
- SQL 写事务（ADR-0128）：BEGIN/SET/DELETE/COMMIT/ROLLBACK；
- 全球读水位联动（ADR-0129）：复制水位 + 陈旧度分位；
- 账单导出（ADR-0130）：Invoice + CSV/JSON + 周期冻结；
- 查询优化（Goal 7）：谓词下推 + 结果缓存；
- 容量模型（Goal 8）：CapacityPlanner + 混沌矩阵。

## 质量

- 全量 **3742/3742 PASS**（+6 容器门控本地跳过）；
- 新增 271 项测试；复制/查询路径零回退；
- 基准见 docs/benchmark/phase30-production-report.md。

## 已知限制

- 跨地域 RTT/RTO/RPO 待 CI/裸机；
- SQL 写事务 COMMIT 回调需接真实 2PC（Phase 31）；
- 负载驱动自动重分片待 Phase 31。

## 升级路径

1. 备份 + PITR 检查点；2. 滚动升级（quorum ≥2）；
3. `recover()` 补完；4. 兼容矩阵验证（ADR-0103）。
