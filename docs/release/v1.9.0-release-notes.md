# Tiering-KV v1.9.0 Release Notes

## 新能力

- 真实执行门禁收敛表 v2 + JVM 级门禁扩展（ADR-0164）；
- 全球自治自学习围栏：成功放宽/失败收紧/回滚熔断（ADR-0165）；
- 物化视图 CDC 增量刷新：INSERT/UPDATE/DELETE + 回退全量
  （ADR-0166）；
- 合规持续证明：SHA-256 哈希链 + 篡改检测（ADR-0167）；
- 多云成本竞价调度：最低成本 + 主权/配额/SLO 约束（ADR-0168）；
- 租户网络策略即代码：声明式 DSL + 幂等编译（ADR-0169）；
- SLO 预算驱动容量决策：达成率 → 扩容建议（ADR-0170）；
- v1.9 发布流水线。

## 兼容性

- v1.0–v1.8 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 36 新增 374 项（surefire 口径）；
- 全量回归 **5660/5660 全绿**（+6 容器门控跳过）。

## 已知限制

- 真实执行门禁（CI 容器/磁盘混沌/kind/release/跨机跨地域）待 Runner；
- 自学习围栏为单指标反馈；
- CDC 增量状态未持久化（重启需全量回退）。
