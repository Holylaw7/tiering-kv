# Tiering-KV v2.1.0 Release Notes

## 新能力

- 真实执行门禁收敛表 v4 + JVM 级门禁扩展（ADR-0178）；
- 远端物化增量状态持久化：CRC 落盘 + 损坏回退（ADR-0179，
  TD-064 关闭）；
- 全球自治策略自进化：简化 Q 学习（ADR-0180）；
- 物化视图生命周期：TTL + 归档恢复（ADR-0181）；
- 合规证明公钥签名：HMAC 覆盖完整 payload（ADR-0182）；
- Spot 中断迁移自动化：确定性迁移计划（ADR-0183）；
- 网络策略风险评分：规则驱动 0~100 + 风险视图（ADR-0184）；
- v2.1.0 发布流水线。

## 兼容性

- v1.0–v2.0 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 38 新增 393 项（surefire 口径）；
- 全量回归 **6433/6433 全绿**（+6 容器门控跳过）。

## 已知限制

- 真实执行门禁（CI 容器/磁盘混沌/kind/release/跨机跨地域）待 Runner；
- 强化学习为单智能体原型；
- 签名密钥无轮换机制。
