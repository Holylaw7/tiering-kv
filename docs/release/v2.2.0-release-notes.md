# Tiering-KV v2.2.0 Release Notes

## 新能力

- 真实执行门禁收敛表 v5 + JVM 级门禁扩展（ADR-0185）；
- 强化学习多智能体自治：本地 Q + 联邦聚合（ADR-0186，TD-067 关闭
  方向）；
- 物化视图远端存储自动分层：热度 → HOT/WARM/COLD（ADR-0187）；
- 合规证明链上锚定：SHA-256 锚定 + 篡改检测（ADR-0188）；
- Spot 市场实时接入与中断率预测：移动平均/指数平滑（ADR-0189，
  TD-065 关闭方向）；
- 策略风险自适应加固：评分驱动撤销 + 审计回滚（ADR-0190）；
- 全局 Pareto 多目标容量优化：支配关系 + 权重选择（ADR-0191）；
- v2.2.0 发布流水线。

## 兼容性

- v1.0–v2.1 冻结协议不变，新能力全部 additive；
- 不修改 Raft safety / MVCC consistency / 事务状态机。

## 测试

- Phase 39 新增 445 项（surefire 口径）；
- 全量回归 **6878/6878 全绿**（+6 容器门控跳过）。

## 已知限制

- 真实执行门禁（CI 容器/磁盘混沌/kind/release/跨机跨地域）待 Runner；
- 多智能体聚合为同步平均；
- Spot 市场为模拟数据源。
