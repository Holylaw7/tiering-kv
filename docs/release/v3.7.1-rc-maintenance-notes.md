# Tiering-KV v3.7.1-rc Maintenance Notes

维护模式首个补丁候选：真实 GitHub Runner 门禁暴露问题的修复集合。
无功能变更，所有修复遵循测试先行 + Conventional Commit。

## 补丁范围

1. **真实 Runner 门禁全绿**：build / test / transaction-e2e / release
   7 个 job 连续两轮全绿（commit `580ae34` → `cd3db80`）；
   TD-048/049（容器/块设备/kind 真实执行）获得真实执行证据。
2. **GHCR 镜像命名**：统一为 `ghcr.io/holylaw7/tiering-kv`
   （owner 必须是 GitHub 用户名全小写；原 `tiering-kv/...` 与
   `Holylaw7/...` 均被 registry 拒绝）。
3. **依赖漏洞**：netty 4.1.115→4.1.136.Final、slf4j 1.7.36→2.0.17、
   logback 1.2.13→1.5.34；Trivy `fs .` 0 漏洞。
4. **容器入口契约**：事务 compose 与 K8s start.sh 显式运行
   `TxnRuntimeMain`（`--node-id` 连字符），避免误入 jar 默认
   Main-Class `ClusterMain`。
5. **CI 稳定化**：
   - `TestPorts` 进程内安全端口分配器（TTL 保留防 TOCTOU），
     16 个 freePort 调用点全部迁移；
   - surefire 失败用例重跑一次（`rerunFailingTestsCount=1`），
     重跑仍失败则门禁照常失败；
   - Docker BuildKit 瞬时 EOF 构建重试；
   - `@Tag("benchmark")` 从功能门禁排除，release Benchmark 步骤
     显式补全 71 个 benchmark 类；
   - build workflow 与全量测试解耦（`-DskipTests`），surefire 堆
     1g→2g。

## 验收标准

- 连续两轮：build/test/transaction-e2e（main+develop）+ release 全绿；
- Trivy 0 漏洞；全量回归 0 failures；
- 镜像推送 `ghcr.io/holylaw7/tiering-kv:v3.7.1` 成功；
- GitHub Release v3.7.1 发布说明覆盖补丁清单。

## 状态

2026-08-14：**v3.7.1 已发布**。

- GitHub Release：v3.7.1（维护发布说明已修正，无 v1.0 模板污染）；
- 镜像：`ghcr.io/holylaw7/tiering-kv:v3.7.1`；
- 门禁：规划提交 `7b5de37` 回归 6/6 全绿；发布前基线连续两轮 7/7；
- 归档：docs/review/phase57-maintenance-review.md（v3.7.1 增补小节）。
