# Linearizability Verification

## 模型

- 操作历史：invoke/response + 类型（GET/PUT）+ 值/结果；
- 线性化点搜索：存在与实时序一致的串行化顺序；
- 违例历史（陈旧读、幽灵读、违反实时序）必须被拒绝。

## 范围

单键读写历史（≤8 全排列；更大历史以 invoke 序为线性化序，登记限制）；
跨键/网络分区验证为后续方向。
